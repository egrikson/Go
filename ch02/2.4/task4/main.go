// Задача 4. Сброс iota *
//
// Объявить два отдельных блока const, в каждом свой набор констант через
// iota (первый: A, B, C; второй: X, Y — оба блока начинаются с 0).
//
// Вывести все пять значений одной строкой одним вызовом fmt.Printf:
//
//	A=0 B=1 C=2 X=0 Y=1
//
// Ограничения: ровно один вызов fmt.Printf; никаких промежуточных
// переменных; значения — только из iota (вручную задаётся только первая
// константа каждого блока).
//
// Запуск: go run ./ch02/2.4/task4
package main

import "fmt"

func main() {

}
