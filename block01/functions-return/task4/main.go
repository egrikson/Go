// Задача 4*. Сумма цифр через рекурсию
//
// Написать функцию digitSum(n int) int, возвращающую сумму цифр числа.
// Проверить на 7, 493, 10, 99999.
//
// Ожидаемый вывод:
//
//	7 -> 7
//	493 -> 16
//	10 -> 1
//	99999 -> 45
//
// Ограничения: НИ ОДНОГО цикла. Функция должна вызывать сама себя —
// это называется рекурсией.
//
// Как к этому подступиться. Сумма цифр числа 493 — это последняя цифра
// (3) плюс сумма цифр того, что осталось (49). А сумма цифр 49 — это
// 9 плюс сумма цифр 4. Задача каждый раз сводится к такой же задаче,
// но меньше. Последняя цифра достаётся через % 10, отбрасывается
// через / 10 — этим ты занимался ещё в задаче про сумму цифр из 2.6,
// только там всё было в одно выражение.
//
// Главный вопрос рекурсии — где остановиться. Функция, которая всегда
// вызывает себя, никогда не завершится и уронит программу. Значит
// нужен случай, при котором ответ известен сразу, без вызова себя.
// Для суммы цифр это однозначное число: его сумма цифр — оно само.
//
// Проверь себя: 10 должно дать 1, а не 0 и не зациклиться.
//
// Запуск: go run ./block01/functions-return/task4
package main

func main() {

}
